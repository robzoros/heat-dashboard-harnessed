rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /championships/{championshipId} {
      allow read: if true;

      allow create, update, delete: if request.auth != null
        && request.auth.uid == 'TRJQfvQ8MjcVw5spX23skZBiSqr1';
    }
  }
}
